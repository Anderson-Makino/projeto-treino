{% extends 'base.html.twig' %}

{% block title %}Funcionario{% endblock %}

{% block body %}
    <h1>Funcionario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ funcionario.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ funcionario.nome }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ funcionario.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ funcionario.phone }}</td>
            </tr>
            <tr>
                <th>Salario</th>
                <td>{{ funcionario.salario }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ funcionario.company }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_funcionario_index') }}">back to list</a>

    <a href="{{ path('app_funcionario_edit', {'id': funcionario.id}) }}">edit</a>

    {{ include('funcionario/_delete_form.html.twig') }}
{% endblock %}
