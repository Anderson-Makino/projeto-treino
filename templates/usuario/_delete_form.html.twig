<form method="post" action="{{ path('app_usuario_delete', {'id': usuario.id}) }}" style= "display:inline;" ,onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.id) }}">
    <button class="btn btn-primary">Deletar</button>
</form>
