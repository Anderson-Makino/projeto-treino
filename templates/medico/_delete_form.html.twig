<form method="post" action="{{ path('app_medico_delete', {'id': medico.id}) }}" style= "display:inline;" ,onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medico.id) }}">
    <button class="btnbig">Deletar</button>
</form>
