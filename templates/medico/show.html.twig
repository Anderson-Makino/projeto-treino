{% extends 'base.html.twig' %}

{% block title %}Medico{% endblock %}

{% block body %}

    {{ block ("navbar", "navbar.html.twig") }}
    <h1>Medico</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medico.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ medico.nome }}</td>
            </tr>
            <tr>
                <th>Crm</th>
                <td>{{ medico.crm }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ medico.phone }}</td>
            </tr>
            <tr>
                <th>Company</th>
                <td>{{ empresa.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medico_index') }}">back to list</a>

    <a href="{{ path('app_medico_edit', {'id': medico.id}) }}">edit</a>

    {{ include('medico/_delete_form.html.twig') }}
{% endblock %}
