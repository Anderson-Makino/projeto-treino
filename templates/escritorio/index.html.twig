{% extends 'base.html.twig' %}

{% block title %}Escritorio index{% endblock %}

{% block body %}

    {{ block ("navbar", "navbar.html.twig") }}
    <h1>Escritorio index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Endereco</th>
                <th>Phone</th>
                <th>Descricao</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for escritorio in escritorios %}
            <tr>
                <td>{{ escritorio.id }}</td>
                <td>{{ escritorio.nome }}</td>
                <td>{{ escritorio.endereco }}</td>
                <td>{{ escritorio.phone }}</td>
                <td>{{ escritorio.descricao }}</td>
                <td>
                    <a href="{{ path('app_escritorio_show', {'id': escritorio.id}) }}">show</a>
                    <a href="{{ path('app_escritorio_edit', {'id': escritorio.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_escritorio_new') }}">Create new</a>
{% endblock %}
