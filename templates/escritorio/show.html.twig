{% extends 'base.html.twig' %}

{% block title %}Escritorio{% endblock %}

{% block body %}

    {{ block ("navbar", "navbar.html.twig") }}
    <h1>Escritorio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ escritorio.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ escritorio.nome }}</td>
            </tr>
            <tr>
                <th>Endereco</th>
                <td>{{ escritorio.endereco }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ escritorio.phone }}</td>
            </tr>
            <tr>
                <th>Descricao</th>
                <td>{{ escritorio.descricao }}</td>
            </tr>
            <tr>
                <th>Empresa</th>
                <td>{{ empresa.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class ="redirect_link btn" href="{{ path('app_escritorio_index') }}">Retornar a Lista</a>

    <a class ="redirect_link btn" href="{{ path('app_escritorio_edit', {'id': escritorio.id}) }}">Editar</a>

    {{ include('escritorio/_delete_form.html.twig') }}
{% endblock %}
