<form method="post" action="{{ path('app_escritorio_delete', {'id': escritorio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ escritorio.id) }}">
    <button class="btn">Deletar</button>
</form>
